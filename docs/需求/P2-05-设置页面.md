# 设置页面

## 基本信息

| 字段 | 值 |
|------|---|
| 功能编号 | P2-05 |
| 所属阶段 | Phase 2 |
| 优先级 | P2 |
| 状态 | ✅ 已完成 |
| 关联文件 | `lib/pages/settings/settings_page.dart` |

## 功能描述

设置页面提供应用配置的查看入口，当前为只读信息展示面板，包含连接配置、外观设置和应用信息三个分区。通过 GlobalBar 的 Settings 按钮打开，内嵌在主布局中（非独立页面）。

## 用户操作流程

1. 点击 GlobalBar 右侧的 Settings 图标
2. 内容区域切换为设置面板（GlobalBar 和 StatusBar 保持不变）
3. 浏览各分区的配置信息
4. 再次点击 Settings 图标或 Home 图标返回抓包页面

## 界面设计

### 布局结构
```
┌──────────────────────────────────────┐
│ Connection                           │
│ ┌──────────────────────────────────┐ │
│ │ API Endpoint    http://localhost:9090 │
│ │ WebSocket       ws://localhost:9090/ws │
│ └──────────────────────────────────┘ │
│                                      │
│ Appearance                           │
│ ┌──────────────────────────────────┐ │
│ │ Theme           Follow system  🔆 │
│ └──────────────────────────────────┘ │
│                                      │
│ About                                │
│ ┌──────────────────────────────────┐ │
│ │ Version         1.0.0-dev        │
│ │ Engine          Swift/NIO + KnotWebService │
│ └──────────────────────────────────┘ │
└──────────────────────────────────────┘
```

### 设计规范
- 整体 ListView + 16px 内边距
- 每个分区: 分区标题（13px, 粗体, hintColor） + 8px 间距 + 圆角卡片容器
- 卡片: surfaceContainerLow 背景 + dividerColor 边框 + 8px 圆角
- 每项: ListTile(dense: true)，title 为标签，trailing 为值（hintColor, 13px）

## 技术实现

### 前端（Flutter）
- **SettingsPanel**: StatelessWidget
- 纯静态展示，无交互逻辑
- 三个分区通过 `_section()` 工厂方法构建
- 信息项通过 `_infoTile()` 工厂方法构建
- Theme 项使用完整 ListTile（title + subtitle + trailing icon）

### 后端（Swift/KnotWebService）
- 无后端交互

## 边界条件与异常处理

- 当前为纯静态页面，无异常情况

## Todo

- [ ] API Endpoint 和 WebSocket 地址应支持用户修改（配置化）
- [ ] Theme 切换应可操作（当前只显示 "Follow system"）
- [ ] 缺少深色/浅色/系统三选一 UI
- [ ] 缺少代理端口配置
- [ ] 缺少 CA 证书管理入口
- [ ] 缺少日志级别设置
- [ ] 缺少数据存储路径配置
- [ ] 缺少清除缓存/重置设置功能
- [ ] 缺少快捷键列表展示
- [ ] Version 硬编码为 "1.0.0-dev"，应从 pubspec 读取
