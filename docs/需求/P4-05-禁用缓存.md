# 禁用缓存（No Caching）

## 基本信息

| 字段 | 值 |
|------|---|
| 功能编号 | P4-05 |
| 所属阶段 | Phase 4 |
| 优先级 | P2 |
| 状态 | ✅ 已完成 |
| 关联文件 | `lib/pages/capture/global_bar.dart`, `lib/controllers/tools_controller.dart` |

## 功能描述

No Caching 功能提供一个全局开关，启用后代理会移除请求和响应中的缓存相关头部，确保每次请求都能获得服务器的最新响应。适用于调试缓存策略或确保看到最新内容的场景。

## 用户操作流程

1. 点击 GlobalBar → Tools 菜单
2. 在菜单中找到 "No Caching" 项（带 checkbox 图标）
3. 点击切换:
   - 启用: checkbox 变为选中状态，SnackBar 提示 "No Caching enabled"
   - 禁用: checkbox 变为未选中状态，SnackBar 提示 "No Caching disabled"

## 界面设计

### Tools 菜单中的展示
```
┌──────────────────────┐
│ 🔀 Map Remote        │
│ 📋 Allow/Block List  │
│ ─────────────────── │
│ ☑️ No Caching         │  ← Checkbox 图标 + 文字
│ ─────────────────── │
│ ...                  │
└──────────────────────┘
```

- 使用 Obx 响应式渲染 checkbox 状态
- 启用时: check_box 图标
- 禁用时: check_box_outline_blank 图标
- 点击后不关闭菜单（但实际 PopupMenuButton 会关闭）

### SnackBar 反馈
- "No Caching enabled" 或 "No Caching disabled"
- 显示时长 2s

## 技术实现

### 前端（Flutter）
- **ToolsController**:
  - `noCachingEnabled: RxBool` — 缓存禁用开关状态
  - `toggleNoCaching()`: 切换布尔值
- **GlobalBar._ToolsMenuButton**:
  - PopupMenuItem(value: 'noCaching')
  - 内部 Obx 动态切换 checkbox 图标
  - onSelected 处理: toggle + SnackBar

### 后端（Swift/KnotWebService）
- 当前仅前端状态管理
- 实际功能需要代理引擎支持: 移除 If-Modified-Since / If-None-Match / Cache-Control 等头部

## 边界条件与异常处理

- 开关状态仅在内存中，重启后重置为关闭
- 菜单 Obx 在 PopupMenu 打开期间动态更新

## Todo

- [ ] 开关状态未持久化
- [ ] 未与代理引擎集成（当前不实际生效）
- [ ] 缺少独立的 UI 指示（如 GlobalBar 上的缓存禁用图标/badge）
- [ ] 缺少选择性禁用（仅特定域名）
- [ ] 缺少具体说明移除哪些缓存头
- [ ] SnackBar 在菜单关闭后才显示，体验不够即时
